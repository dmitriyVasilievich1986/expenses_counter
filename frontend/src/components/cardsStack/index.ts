/**
 * Re-exports the `CardsStack` component and the `ItemType` shape for card items.
 */

import { CardsStack } from './CardsStack';
import { type ItemType } from './types';

export { CardsStack, type ItemType };
