/**
 * Barrel module for the address API client: re-exports {@link useAddressAPIClient} from `./client`.
 *
 * @module services/apiClient/address/index
 */

import { useAddressAPIClient } from './client';

export { useAddressAPIClient };
